/** @deprecated Import from locationData.js — kept for backward compatibility */
export {
  COUNTRY_NAMES as COUNTRIES,
  filterCountries,
  getCountryLabel,
  resolveCountryCode,
} from "./locationData.js";

